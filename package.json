{
  "name": "freeapp",
  "version": "1.0.0",
  "main": "marked.min.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "vercel dev",
    "build": "node scripts/build-env-inject.js",
    "build:dev": "ENVIRONMENT=development node scripts/build-env-inject.js",
    "build:staging": "ENVIRONMENT=staging node scripts/build-env-inject.js",
    "build:prod": "ENVIRONMENT=production node scripts/build-env-inject.js",
    "build:single": "node scripts/build-single-file.js",
    "build:single-clean": "CREATE_TIMESTAMP=false node scripts/build-single-file.js",
    "build:single-only": "vite build --config vite.config.js",
    "build:single-debug": "vite build --config vite.config.js --logLevel info",
    "test:single": "node scripts/test-single-file.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "pg": "^8.11.0",
    "node-fetch": "^2.6.7"
  },
  "devDependencies": {
    "vercel": "^32.0.0",
    "vite": "^5.0.0",
    "vite-plugin-singlefile": "^2.0.0"
  }
}
